"use client"
import { ProductType } from '@/types'
import React from 'react'

const ProductCard = ({product}:{product:ProductType}) => { //take product from parent of productType 
  return (
    <div>ProductCard</div>
  )
}

export default ProductCard